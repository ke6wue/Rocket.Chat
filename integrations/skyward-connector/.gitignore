# Secrets — never commit real Skyward or Rocket.Chat credentials
.env

# Local mapping/audit SQLite DB — contains student/staff/parent account
# activity history. Treat as sensitive; it does not belong in git history
# even on a private repo.
data/
*.db
*.db-journal
*.db-wal
*.db-shm

# Dependencies and build output
node_modules/
dist/

# Logs
*.log
npm-debug.log*

# Editor/OS cruft
.DS_Store
.vscode/
.idea/
